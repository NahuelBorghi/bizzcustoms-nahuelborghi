import { useState } from "react";

export const Loader = () =>{
    const [flag,setFlag] = useState(false)
    
    if(flag == ){
        return(

        );
    }else{
        return(

        );
    }
}